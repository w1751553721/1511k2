<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bwie.wangkui.mynewjdshopping">

    <!-- 网络连接 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 网络判断 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 文件写入 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 文件读取 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo_web"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleInstance" />
        <activity android:name=".FirstLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".myinfo.view.LoginPage" />
        <activity android:name=".myinfo.view.RegisterActivity" />
        <activity
            android:name=".homepage.view.F1SeekActivity"
            android:launchMode="singleInstance" />
        <activity android:name=".GoodsSeek.DataViewActivity" />
        <activity android:name=".goods_details.view.GoodsDatails" />
        <activity android:name=".myinfo.view.InfoSetting" />
        <activity android:name=".goods_details.view.ArrirmOrder" />
        <activity android:name=".goods_details.view.ConfirmPment"></activity>

    </application>

</manifest>